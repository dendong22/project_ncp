services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "80:8080"
    env_file:
      - .env
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=8080
      - PASS2_PROVIDER=${PASS2_PROVIDER:-hcx}
    volumes:
      - ./data:/app/data
